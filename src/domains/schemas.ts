import { z } from "zod";

export const RoleSchema = z.enum(["Super Admin", "Managing Director", "Accounts", "Import Officer", "Warehouse Manager", "Sales Manager", "Sales Executive"]);
export const PermissionActionSchema = z.enum(["view", "create", "edit", "delete", "approve", "post", "export"]);
export const PermissionKeySchema = z.enum(["dashboard", "users", "suppliers", "customers", "products", "import", "inventory", "sales", "accounts", "reports", "settings", "print"]);
export const PermissionOverrideSchema = z.object({ permission: PermissionKeySchema, action: PermissionActionSchema, effect: z.enum(["ALLOW", "DENY"]) });
export const CapabilitySchema = z.enum(["view_sensitive_cost", "edit_sensitive_cost", "finalize_landed_cost", "reopen_landed_cost", "view_profit", "approve_stock_override", "manage_users", "manage_user_access", "approve_special_price"]);
export const UserSchema = z.object({ id: z.string(), name: z.string(), email: z.string(), role: RoleSchema, title: z.string(), department: z.string(), phone: z.string(), avatarUrl: z.string(), status: z.enum(["Active", "Pending", "Inactive"]), territory: z.string().optional(), employeeCode: z.string().optional(), permissionOverrides: z.array(PermissionOverrideSchema).optional(), capabilities: z.array(CapabilitySchema).optional() });
export const ProductSchema = z.object({ id: z.string(), code: z.string(), family: z.string(), variant: z.string(), name: z.string(), unit: z.string(), hsCode: z.string().optional(), imageUrl: z.string(), standardSalePrice: z.string(), active: z.boolean() });
export const SupplierSchema = z.object({ id: z.string(), name: z.string(), country: z.string(), contactPerson: z.string(), phone: z.string(), email: z.string(), paymentTerms: z.string(), active: z.boolean() });
export const DocumentRecordSchema = z.object({ id: z.string(), entityType: z.enum(["import", "import-cost", "expense"]), entityId: z.string(), documentType: z.string(), source: z.enum(["UPLOADED", "GENERATED"]), fileName: z.string(), mimeType: z.string(), sizeBytes: z.number().optional(), previewUrl: z.string(), sensitive: z.boolean(), notes: z.string().optional(), createdByUserId: z.string(), createdByName: z.string(), createdAt: z.string() });
export const ImportItemSchema = z.object({ id: z.string(), productId: z.string(), productCode: z.string(), productName: z.string(), quantity: z.string(), unit: z.string(), currency: z.string(), fobUnitForeign: z.string(), exchangeRate: z.string(), fobTotalBdt: z.string(), cbmPerCarton: z.string(), cartonCount: z.string(), totalCbm: z.string(), cbmMode: z.enum(["CALCULATED", "MANUAL"]).optional(), grossWeight: z.string().optional(), netWeight: z.string().optional(), hsCode: z.string().optional() });
export const ImportCostLineSchema = z.object({ id: z.string(), name: z.string(), category: z.string(), amountForeign: z.string(), currency: z.string(), exchangeRate: z.string(), amountBdt: z.string(), allocationMethod: z.enum(["CBM", "FOB_VALUE", "QUANTITY", "PRODUCT_SPECIFIC", "MANUAL"]), appliesToItemIds: z.array(z.string()), manualSplits: z.array(z.object({ importItemId: z.string(), amountBdt: z.string() })).optional(), vendor: z.string().optional(), paymentDate: z.string().optional(), accountId: z.string().optional(), notes: z.string().optional(), attachmentName: z.string().optional(), attachment: DocumentRecordSchema.optional(), enteredBy: z.string(), createdAt: z.string() });
export const ImportDocumentSchema = DocumentRecordSchema.extend({ importId: z.string(), type: z.string(), name: z.string(), uploadedAt: z.string(), uploadedBy: z.string(), status: z.enum(["Available", "Pending"]) });
export const CostAllocationSchema = z.object({ costLineId: z.string(), costName: z.string(), importItemId: z.string(), productName: z.string(), method: z.enum(["CBM", "FOB_VALUE", "QUANTITY", "PRODUCT_SPECIFIC", "MANUAL"]), basisValue: z.string(), sharePercent: z.string(), allocatedBdt: z.string(), perUnitBdt: z.string(), explanation: z.string() });
export const LandedCostProductResultSchema = z.object({ importItemId: z.string(), productCode: z.string(), productName: z.string(), quantity: z.string(), fobTotalBdt: z.string(), fobPerUnitBdt: z.string(), additionalCostBdt: z.string(), additionalPerUnitBdt: z.string(), finalTotalBdt: z.string(), finalPerUnitBdt: z.string(), components: z.array(CostAllocationSchema) });
export const LandedCostPreviewSchema = z.object({ importId: z.string(), totalProductValueBdt: z.string(), totalAdditionalCostBdt: z.string(), totalShipmentCostBdt: z.string(), products: z.array(LandedCostProductResultSchema), allocations: z.array(CostAllocationSchema), validationErrors: z.array(z.string()) });
export const LandedCostSnapshotSchema = LandedCostPreviewSchema.extend({ id: z.string(), version: z.number(), finalizedAt: z.string(), finalizedBy: z.string(), immutable: z.literal(true) });
export const ImportCaseSchema = z.object({
  id: z.string(), draftReference: z.string(), primaryReference: z.string(), supplierId: z.string(), supplierName: z.string(), poNumber: z.string(), poDate: z.string(), piNumber: z.string().optional(), piDate: z.string().optional(), paymentMode: z.enum(["Pending", "LC", "TT"]), lcNumber: z.string().optional(), lcAmount: z.string().optional(), lcOpenDate: z.string().optional(), lcExpiryDate: z.string().optional(), ttReference: z.string().optional(), ttAmount: z.string().optional(), ttDate: z.string().optional(), bank: z.string().optional(), currency: z.string(), exchangeRate: z.string(), rateDate: z.string(), rateSource: z.string(), expectedShipmentDate: z.string().optional(), commercialInvoiceNumber: z.string().optional(), commercialInvoiceDate: z.string().optional(), productionStatus: z.string().optional(), productionFollowUpNote: z.string().optional(), blNumber: z.string().optional(), containerNumber: z.string().optional(), containerType: z.string().optional(), vesselName: z.string().optional(), etd: z.string().optional(), eta: z.string().optional(), status: z.enum(["Draft", "PI Received", "LC/TT Opened", "In Production", "Shipped", "At Port", "Costing", "Cost Finalized", "Partially Received", "Received", "Closed", "Cancelled"]), milestone: z.enum(["Draft", "PI Received", "LC/TT Opened", "In Production", "Shipped", "At Port", "Costing", "Cost Finalized", "Partially Received", "Received", "Closed", "Cancelled"]), costingStatus: z.enum(["Not Started", "In Progress", "Ready", "Finalized"]), warehouseStatus: z.enum(["Not Ready", "Ready", "Partially Received", "Received"]), notes: z.string().optional(), items: z.array(ImportItemSchema), costs: z.array(ImportCostLineSchema), documents: z.array(ImportDocumentSchema), snapshot: LandedCostSnapshotSchema.optional(), snapshotHistory: z.array(LandedCostSnapshotSchema).optional(), createdAt: z.string(), updatedAt: z.string()
});
export const WarehouseReceiptLineSchema = z.object({ importItemId: z.string(), productId: z.string(), productName: z.string(), quantityReceived: z.string(), quantityRejected: z.string(), lotNumber: z.string(), batchNumber: z.string(), manufacturingDate: z.string(), expiryDate: z.string(), warehouse: z.string(), location: z.string().optional(), landedCostPerUnit: z.string() });
export const WarehouseReceiptSchema = z.object({ id: z.string(), importId: z.string(), reference: z.string(), receivedDate: z.string(), lines: z.array(WarehouseReceiptLineSchema), status: z.literal("Posted"), receivedBy: z.string() });
export const StockBatchSchema = z.object({ id: z.string(), productId: z.string(), productCode: z.string(), productName: z.string(), sourceImportId: z.string(), sourceReference: z.string(), lotNumber: z.string(), batchNumber: z.string(), manufacturingDate: z.string(), expiryDate: z.string(), receivedDate: z.string(), quantityReceived: z.string(), quantityAvailable: z.string(), warehouse: z.string(), location: z.string().optional(), landedCostPerUnit: z.string(), sourceType: z.enum(["Import Receipt", "Opening Stock"]).optional(), expiryStatus: z.enum(["Expired", "1 Month Alert", "3 Month Alert", "6 Month Alert", "Normal"]).optional() });
export const StockMovementSchema = z.object({ id: z.string(), date: z.string(), productId: z.string(), productName: z.string(), batchId: z.string(), batchNumber: z.string(), type: z.enum(["Receive", "Dispatch", "Return", "Adjustment"]), quantity: z.string(), reference: z.string(), reason: z.string().optional(), createdBy: z.string() });
export const StockSummarySchema = z.object({ productId: z.string(), productCode: z.string(), productName: z.string(), imageUrl: z.string(), availableQuantity: z.string(), oldestLot: z.string(), nearestExpiry: z.string(), standardSalePrice: z.string(), inventoryValue: z.string().optional() });
export const CustomerSchema = z.object({ id: z.string(), name: z.string(), type: z.enum(["Hospital", "Clinic", "Dealer", "Pharmacy", "Other"]), contactPerson: z.string(), phone: z.string(), address: z.string(), territory: z.string(), assignedSalesUserId: z.string().optional(), paymentTerms: z.string(), creditLimit: z.string(), currentDue: z.string(), totalSales: z.string(), totalCollected: z.string(), active: z.boolean() });
export const SalesLineSchema = z.object({ id: z.string(), productId: z.string(), productCode: z.string(), productName: z.string(), quantity: z.string(), unitPrice: z.string(), discount: z.string(), lineTotal: z.string() });
const CustomerSnapshotFields = { customerAddressSnapshot: z.string().optional(), customerPhoneSnapshot: z.string().optional(), customerContactSnapshot: z.string().optional() };
export const QuotationSchema = z.object({ id: z.string(), quotationNumber: z.string(), date: z.string(), customerId: z.string(), customerName: z.string(), ...CustomerSnapshotFields, ownerId: z.string(), createdByUserId: z.string().optional(), validityDays: z.number(), paymentTerms: z.string(), remarks: z.string().optional(), status: z.enum(["Draft", "Sent", "Accepted", "Rejected", "Converted"]), lines: z.array(SalesLineSchema), subtotal: z.string(), discountTotal: z.string(), total: z.string() });
export const SalesOrderSchema = z.object({ id: z.string(), orderNumber: z.string(), quotationId: z.string().optional(), date: z.string(), customerId: z.string(), customerName: z.string(), ...CustomerSnapshotFields, ownerId: z.string(), createdByUserId: z.string().optional(), paymentConditions: z.string(), deliveryInstruction: z.string(), amountReceived: z.string(), due: z.string(), status: z.enum(["Placed", "Ready", "Partially Delivered", "Delivered", "Cancelled"]), lines: z.array(SalesLineSchema), total: z.string(), paymentConfirmation: z.string().optional(), paymentReference: z.string().optional(), paymentDate: z.string().optional(), requestedDeliveryDate: z.string().optional(), orderReceivedByName: z.string().optional(), orderReceivedByDesignation: z.string().optional(), orderGivenBy: z.string().optional(), headOfSalesSignoff: z.string().optional(), coeSignoff: z.string().optional(), mdSignoff: z.string().optional() });
export const DeliveryLineSchema = SalesLineSchema.extend({ batchId: z.string(), batchNumber: z.string() });
export const DeliverySchema = z.object({ id: z.string(), challanNumber: z.string(), orderId: z.string(), customerId: z.string(), customerName: z.string(), salesOwnerId: z.string().optional(), dispatchedByUserId: z.string().optional(), date: z.string(), remarks: z.string(), receiverName: z.string().optional(), overrideReason: z.string().optional(), status: z.enum(["Draft", "Dispatched", "Delivered"]), lines: z.array(DeliveryLineSchema) });
export const CollectionSchema = z.object({ id: z.string(), receiptNumber: z.string(), customerId: z.string(), customerName: z.string(), orderId: z.string().optional(), date: z.string(), amount: z.string(), paymentMode: z.enum(["Cash", "bKash", "Bank Transfer", "Cheque"]), accountId: z.string().optional(), referenceNumber: z.string().optional(), remarks: z.string().optional(), ownerId: z.string(), postedByUserId: z.string().optional(), status: z.enum(["Posted", "Reversed"]) });
export const ExpenseSchema = z.object({ id: z.string(), date: z.string(), categoryId: z.string(), categoryName: z.string(), subtype: z.enum(["General", "TA/DA"]), amount: z.string(), paidFromAccountId: z.string(), employee: z.string().optional(), designation: z.string().optional(), taAmount: z.string().optional(), daAmount: z.string().optional(), remarks: z.string(), attachmentName: z.string().optional(), attachment: DocumentRecordSchema.optional(), status: z.enum(["Posted", "Reversed"]) });
export const AccountSchema = z.object({ id: z.string(), name: z.string(), type: z.enum(["Cash", "Bank", "Mobile Banking"]), accountNumber: z.string().optional(), balance: z.string(), active: z.boolean() });
export const AccountTransactionSchema = z.object({ id: z.string(), date: z.string(), accountId: z.string(), accountName: z.string(), direction: z.enum(["In", "Out"]), amount: z.string(), sourceType: z.enum(["Collection", "Expense", "Import Cost", "Opening Balance"]), sourceId: z.string(), description: z.string() });
export const AuditEventSchema = z.object({ id: z.string(), timestamp: z.string(), userId: z.string(), userName: z.string(), role: RoleSchema, action: z.string(), entityType: z.string(), entityId: z.string(), summary: z.string(), reason: z.string().optional() });
export const BusinessDecisionSchema = z.object({ id: z.string(), title: z.string(), question: z.string(), currentBehavior: z.string(), status: z.enum(["Pending Client Confirmation", "Confirmed"]), blocks: z.array(z.string()), resolutionValue: z.string().optional(), resolutionNotes: z.string().optional(), confirmedBy: z.string().optional(), confirmedAt: z.string().optional(), sourceReference: z.string().optional() });
export const DashboardSchema = z.object({ role: RoleSchema, metrics: z.array(z.object({ id: z.string(), label: z.string(), value: z.string(), unit: z.string(), sensitive: z.boolean().optional() })).max(6), importAttention: z.array(ImportCaseSchema), expiryAlerts: z.array(StockBatchSchema), customerDues: z.array(CustomerSchema), recentSales: z.array(SalesOrderSchema), recentCollections: z.array(CollectionSchema), recentExpenses: z.array(ExpenseSchema) });
const ReportRowSchema = z.object({ label: z.string(), value: z.string() });
export const ReportTableSchema = z.object({ id: z.string(), title: z.string(), columns: z.array(z.object({ key: z.string(), label: z.string(), align: z.enum(["left", "right"]).optional() })), rows: z.array(z.record(z.string(), z.string())) });
export const ReportSchema = z.object({ period: z.object({ from: z.string(), to: z.string() }), importCosts: z.array(ReportRowSchema), inventory: z.array(ReportRowSchema), sales: z.array(ReportRowSchema), expenses: z.array(ReportRowSchema), tables: z.object({ imports: z.array(ReportTableSchema), inventory: z.array(ReportTableSchema), sales: z.array(ReportTableSchema), expenses: z.array(ReportTableSchema) }) });
export const SalespersonEmployeeSchema = z.object({ id: z.string(), name: z.string(), title: z.string(), territory: z.string().optional(), employeeCode: z.string().optional() });
export const SalespersonPerformanceSummarySchema = z.object({ quotationsCreated: z.number(), quotationValue: z.string(), sentQuotations: z.number(), acceptedQuotations: z.number(), convertedQuotations: z.number(), rejectedQuotations: z.number(), pendingQuotationValue: z.string(), ordersCreated: z.number(), ordersDelivered: z.number(), deliveredSalesValue: z.string(), unitsDelivered: z.string(), collectionsReceived: z.string(), customersHandled: z.number(), assignedCustomerDue: z.string(), averageOrderValue: z.string(), conversionRate: z.string(), totalDiscount: z.string(), averageDiscount: z.string() });
export const SalespersonComparisonRowSchema = SalespersonEmployeeSchema.merge(SalespersonPerformanceSummarySchema);
export const SalespersonPerformanceDetailSchema = z.object({ employee: SalespersonEmployeeSchema, summary: SalespersonPerformanceSummarySchema, tables: z.object({ quotations: ReportTableSchema, orders: ReportTableSchema, deliveries: ReportTableSchema, collections: ReportTableSchema, customers: ReportTableSchema, products: ReportTableSchema }) });
export const SalespersonPerformanceSchema = z.object({ period: z.object({ from: z.string(), to: z.string() }), selectedEmployeeId: z.string(), employees: z.array(SalespersonEmployeeSchema), comparison: z.array(SalespersonComparisonRowSchema), selected: SalespersonPerformanceDetailSchema.optional() });
export const TrackingStatusSchema = z.enum(["LIVE", "RECENT", "STALE", "OFFLINE", "NOT_TRACKING"]);
export const LocationSourceSchema = z.enum(["MOBILE_APP", "WEB_FOREGROUND", "MANUAL", "DEMO"]);
export const FieldEmployeeSchema = SalespersonEmployeeSchema.extend({ employeeCode: z.string(), phone: z.string(), avatarUrl: z.string() });
export const FieldVisitSchema = z.object({ id: z.string(), userId: z.string(), customerId: z.string(), customerName: z.string(), purpose: z.string(), outcome: z.string().optional(), status: z.enum(["Planned", "Checked In", "Completed", "Missed"]), plannedAt: z.string(), checkInAt: z.string().optional(), checkOutAt: z.string().optional(), customerLatitude: z.number(), customerLongitude: z.number(), checkInLatitude: z.number().optional(), checkInLongitude: z.number().optional(), checkInAccuracyMeters: z.number().optional() });
export const CurrentEmployeeLocationSchema = z.object({ userId: z.string(), employee: FieldEmployeeSchema, latitude: z.number(), longitude: z.number(), accuracyMeters: z.number(), recordedAt: z.string(), status: TrackingStatusSchema, source: LocationSourceSchema, sessionId: z.string().optional(), sessionStartedAt: z.string().optional(), currentVisit: FieldVisitSchema.optional() });
export const LocationHistoryPointSchema = z.object({ id: z.string(), userId: z.string(), latitude: z.number(), longitude: z.number(), accuracyMeters: z.number(), recordedAt: z.string(), source: LocationSourceSchema, event: z.enum(["TRACKING_STARTED", "LOCATION", "VISIT_CHECK_IN", "VISIT_CHECK_OUT", "TRACKING_ENDED"]).optional() });
export const TrackingSessionSchema = z.object({ id: z.string(), userId: z.string(), startedAt: z.string(), endedAt: z.string().optional(), source: LocationSourceSchema, status: z.enum(["Active", "Completed"]) });
export const FieldTeamCurrentSchema = z.object({ feedLabel: z.literal("Demo location feed"), generatedAt: z.string(), employees: z.array(FieldEmployeeSchema), locations: z.array(CurrentEmployeeLocationSchema), summary: z.object({ activeNow: z.number(), recent: z.number(), offline: z.number(), notTracking: z.number(), visitsToday: z.number() }) });
export const FieldTeamHistorySchema = z.object({ feedLabel: z.literal("Demo location feed"), employee: FieldEmployeeSchema, date: z.string(), session: TrackingSessionSchema.optional(), points: z.array(LocationHistoryPointSchema), visits: z.array(FieldVisitSchema) });
export const LocationUpdateInputSchema = z.object({ latitude: z.number().min(-90).max(90), longitude: z.number().min(-180).max(180), accuracyMeters: z.number().nonnegative(), recordedAt: z.string(), source: z.enum(["MOBILE_APP", "WEB_FOREGROUND", "MANUAL"]) });
export const AISourceSchema = z.object({ label: z.string(), path: z.string() });
export const AIChatResponseSchema = z.object({ answer: z.string(), contextLabel: z.string(), suggestions: z.array(z.string()), sources: z.array(AISourceSchema), restricted: z.boolean() });
export const AIInsightSchema = z.object({ id: z.string(), title: z.string(), summary: z.string(), severity: z.enum(["Info", "Attention", "Critical"]), sourceLabel: z.string(), sourcePath: z.string().optional() });
export const AIRecommendationSchema = AIInsightSchema.extend({ reason: z.string(), recommendedAction: z.string(), category: z.enum(["Imports", "Inventory", "Sales", "Collections", "Finance", "Field Team"]).optional(), detectedAt: z.string().optional() });
export const AIDocumentExtractionSchema = z.object({ importId: z.string(), documentId: z.string(), documentName: z.string(), requiresReview: z.literal(true), fields: z.array(z.object({ key: z.string(), label: z.string(), value: z.string(), target: z.enum(["import", "item"]), targetId: z.string().optional(), confidence: z.string() })), warnings: z.array(z.string()) });
export const ExpenseCategorySchema = z.object({ id: z.string(), name: z.string(), active: z.boolean() });
export const WarehouseConfigSchema = z.object({ id: z.string(), name: z.string(), address: z.string(), contactPerson: z.string(), phone: z.string(), locations: z.array(z.string()), active: z.boolean() });
export const CostPresetSchema = z.object({ id: z.string(), name: z.string(), category: z.string(), suggestedAllocationMethod: z.enum(["CBM", "FOB_VALUE", "QUANTITY", "PRODUCT_SPECIFIC", "MANUAL"]).optional(), requiresExplicitChoice: z.boolean(), active: z.boolean() });
export const PrintIdentitySchema = z.object({ id: z.enum(["mipro", "led-trackers"]), companyName: z.string(), displayName: z.string(), address: z.string(), phone: z.string(), email: z.string(), website: z.string(), logoUrl: z.string(), backgroundImageUrl: z.string(), footerText: z.string(), authorizedSignatory: z.string(), safeArea: z.object({ topMm: z.number(), rightMm: z.number(), bottomMm: z.number(), leftMm: z.number() }) });
export const PrintConfigurationSchema = z.object({ identities: z.array(PrintIdentitySchema), defaultIdentityId: z.enum(["mipro", "led-trackers"]), defaultLetterheadMode: z.enum(["Digital", "Preprinted"]) });
export const ProductAliasSchema = z.object({ id: z.string(), aliasText: z.string(), productId: z.string(), productName: z.string(), source: z.string(), active: z.boolean() });
export const CustomerLedgerEntrySchema = z.object({ id: z.string(), date: z.string(), type: z.enum(["Opening Due", "Delivery", "Collection"]), reference: z.string(), debit: z.string(), credit: z.string(), runningDue: z.string(), remarks: z.string() });
export const CustomerLedgerSchema = z.object({ customer: CustomerSchema, deliveredSales: z.string(), collected: z.string(), currentDue: z.string(), entries: z.array(CustomerLedgerEntrySchema), deliveries: z.array(DeliverySchema), collections: z.array(CollectionSchema) });
export const CustomerOpeningBalanceSchema = z.object({ id: z.string(), customerId: z.string(), customerName: z.string(), date: z.string(), openingDue: z.string(), historicalSales: z.string(), historicalCollected: z.string(), reference: z.string(), remarks: z.string(), createdBy: z.string(), createdAt: z.string() });
export const DispatchAllocationSchema = z.object({ batchId: z.string(), batchNumber: z.string(), lotNumber: z.string(), quantity: z.string(), receivedDate: z.string(), expiryDate: z.string() });
export const DispatchPreviewSchema = z.object({ productId: z.string(), requestedQuantity: z.string(), availableQuantity: z.string(), allocations: z.array(DispatchAllocationSchema), warning: z.string().optional(), requiresOverride: z.boolean() });
export const ProfitPreviewSchema = z.object({ lines: z.array(z.object({ productId: z.string(), productCode: z.string(), productName: z.string(), quantity: z.string(), proposedUnitPrice: z.string(), effectiveUnitPrice: z.string(), expectedCostPerUnit: z.string(), grossProfitPerUnit: z.string(), grossProfitTotal: z.string(), marginPercent: z.string(), stockCoverage: z.string(), isLoss: z.boolean() })), revenue: z.string(), expectedCogs: z.string(), grossProfit: z.string(), marginPercent: z.string() });
